<?xml version="1.0" encoding="utf-8"?>
<!--
    Detail page layout shared by all 4 topics.
    Uses ConstraintLayout with:
    - Full-width banner background
    - Overlay for text readability
    - Dynamic icon and label on banner
    - Scrollable content area
-->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Background banner (same for all pages) -->
    <ImageView
        android:id="@+id/img_detail_background"
        android:layout_width="0dp"
        android:layout_height="200dp"
        android:src="@drawable/img_main_banner"
        android:scaleType="centerCrop"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />


    <!-- Large white topic label-->
    <View
        android:id="@+id/overlay"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="#80000000"
        app:layout_constraintBottom_toBottomOf="@id/img_detail_background"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@id/img_detail_background"
        app:layout_constraintVertical_bias="0.0" />

    <ImageView
        android:id="@+id/img_detail_icon"
        android:layout_width="157dp"
        android:layout_height="143dp"
        android:layout_marginStart="24dp"
        android:scaleType="fitCenter"
        app:layout_constraintBottom_toBottomOf="@id/img_detail_background"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@id/img_detail_background" />



    <TextView
        android:id="@+id/txt_detail_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="40dp"
        android:text="@string/label_facilities"
        android:textColor="#FFFFFF"
        android:textSize="28sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="@id/img_detail_background"
        app:layout_constraintStart_toEndOf="@id/img_detail_icon"
        app:layout_constraintTop_toTopOf="@id/img_detail_background" />

    <TextView
        android:id="@+id/txt_detail_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="@string/title_facilities"
        android:textSize="24sp"
        android:textStyle="bold"
        android:gravity="center"
        android:padding="16dp"
        app:layout_constraintTop_toBottomOf="@id/img_detail_background"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />


    <ScrollView
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/txt_detail_title"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:id="@+id/txt_detail_content"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/content_facilities"
            android:textSize="16sp"
            android:lineSpacingExtra="6dp"
            android:padding="16dp" />
    </ScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>